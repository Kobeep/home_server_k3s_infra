apiVersion: v1
kind: Service
metadata:
  name: dashy-service
  namespace: dashboard
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30002
  selector:
    app: dashy
